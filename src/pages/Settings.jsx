// src/pages/Settings.jsx

import './Settings.css'; // 전용 스타일 import

const Settings = () => {
  return (
    <div className="settings-page">
      <h2>설정</h2>
      <p>알림, 테마, 언어 설정 등을 변경할 수 있습니다.</p>
    </div>
  );
};

export default Settings;
